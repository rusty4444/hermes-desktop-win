<Page
    x:Class="HermesDesktop.WinUI.Views.SkillsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HermesDesktop.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:HermesDesktop.WinUI.ViewModels"
    xmlns:models="using:HermesDesktop.WinUI.Models"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:SkillsViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="20">
            <TextBlock Text="Skills" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,10"/>
            <Button Content="Refresh" Click="RefreshButton_Click" Margin="12,0,0,0"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Margin="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>

                <!-- Skills List -->
                <Border Grid.Column="0" BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                        BorderThickness="1" CornerRadius="4" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Installed Skills" Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Margin="12,8,12,8" Grid.Row="0"/>

                        <ListView Grid.Row="1"
                                  ItemsSource="{x:Bind ViewModel.Skills, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedSkill, Mode=TwoWay}"
                                  SelectionChanged="SkillsList_SelectionChanged"
                                  Margin="12,0,12,0">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:SkillInfo">
                                    <StackPanel Margin="0,6,0,6">
                                        <TextBlock Text="{x:Bind Title}" Style="{ThemeResource BodyTextBlockStyle}"
                                                   FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind Description}" Style="{ThemeResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource SystemControlDisabledBaseMediumBrush}"
                                                   Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{x:Bind Path}" Style="{ThemeResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource SystemControlDisabledBaseMediumBrush}"
                                                   Margin="0,2,0,0" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="12,8,12,12" Spacing="8">
                            <Button Content="New Skill" Click="NewSkill_Click"/>
                            <Button Content="Delete" Click="DeleteSkill_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Skill Editor -->
                <Border Grid.Column="1" BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                        BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Editor Header -->
                        <Border Grid.Row="0" Background="{ThemeResource SystemControlAcrylicElementBrush}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock x:Name="SkillTitleText" Text="Select a skill to view/edit"
                                           Style="{ThemeResource SubtitleTextBlockStyle}" Grid.Column="0"/>
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                                    <Button Content="Save" Click="SaveSkill_Click"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <!-- Editor Content -->
                        <ScrollViewer Grid.Row="1" Margin="12">
                            <TextBox x:Name="ContentTextBox"
                                     Text="{x:Bind ViewModel.SkillContent, Mode=TwoWay}"
                                     AcceptsReturn="True"
                                     TextWrapping="NoWrap"
                                     FontFamily="Consolas"
                                     FontSize="13"
                                     Margin="0,0,0,12"
                                     IsSpellCheckEnabled="False"/>
                        </ScrollViewer>

                        <!-- Status Bar -->
                        <Grid Grid.Row="2" Margin="12,0,12,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="0" Spacing="8">
                                <TextBlock Text="{x:Bind ViewModel.ErrorMessage}"
                                           TextWrapping="Wrap"
                                           Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}"
                                           Visibility="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
                                <TextBlock Text="Loading..."
                                           Visibility="{x:Bind ViewModel.IsContentLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <ProgressBar IsIndeterminate="True" Width="100"
                                             Visibility="{x:Bind ViewModel.IsContentLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <TextBlock Text="Saving..."
                                           Visibility="{x:Bind ViewModel.IsContentSaving, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <ProgressBar IsIndeterminate="True" Width="100"
                                             Visibility="{x:Bind ViewModel.IsContentSaving, Converter={StaticResource BoolToVisibilityConverter}}"/>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
