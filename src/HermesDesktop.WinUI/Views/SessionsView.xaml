<Page
    x:Class="HermesDesktop.WinUI.Views.SessionsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HermesDesktop.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:HermesDesktop.WinUI.ViewModels"
    xmlns:models="using:HermesDesktop.WinUI.Models"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:SessionsViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="20">
            <TextBlock Text="Sessions" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Margin="20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <!-- Sessions List -->
                <Border Grid.Column="0" BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1" CornerRadius="4" Margin="0,0,10,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <StackPanel Orientation="Horizontal" Margin="12" Grid.Row="0">
                            <TextBlock Text="Sessions" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                            <Button Content="Refresh" Click="RefreshButton_Click" Margin="12,0,0,0" HorizontalAlignment="Right"/>
                        </StackPanel>

                        <ListView Grid.Row="1"
                                  ItemsSource="{x:Bind ViewModel.Sessions, Mode=OneWay}"
                                  SelectionChanged="SessionsList_SelectionChanged"
                                  Margin="12,0,12,12">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:SessionInfo">
                                    <StackPanel Margin="0,8,0,8">
                                        <TextBlock Text="{x:Bind Title}" Style="{ThemeResource BodyTextBlockStyle}" FontWeight="SemiBold"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="{x:Bind Timestamp, Converter={StaticResource TimestampToStringConverter}}" Style="{ThemeResource CaptionTextBlockStyle}" Margin="0,0,8,0"/>
                                            <TextBlock Text="{x:Bind MessageCount}" Style="{ThemeResource CaptionTextBlockStyle}"/>
                                            <TextBlock Text=" messages" Style="{ThemeResource CaptionTextBlockStyle}" Margin="4,0,0,0"/>
                                        </StackPanel>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <!-- Transcript Viewer -->
                <Border Grid.Column="1" BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Transcript Header -->
                        <Border Grid.Row="0" Background="{ThemeResource SystemControlAcrylicElementBrush}" Padding="12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Orientation="Horizontal" Grid.Column="0">
                                    <TextBlock x:Name="TranscriptTitle" Text="Select a session to view transcript" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                                </StackPanel>
                                <Button x:Name="ResumeButton" Content="Resume in Terminal" Click="ResumeButton_Click" Grid.Column="1" Margin="0,0,12,0" Visibility="Collapsed"/>
                            </Grid>
                        </Border>

                        <!-- Transcript Content -->
                        <ScrollViewer Grid.Row="1" Margin="12">
                            <ItemsControl x:Name="TranscriptItems" Margin="0,0,0,12">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:SessionMessage">
                                        <StackPanel Margin="0,0,0,8">
                                            <Border Background="{x:Bind Role, Converter={StaticResource RoleToBackgroundConverter}}" CornerRadius="4" Padding="8" Margin="0,0,0,4">
                                                <TextBlock Text="{x:Bind Content}" TextWrapping="Wrap" Style="{ThemeResource BodyTextBlockStyle}"/>
                                            </Border>
                                            <TextBlock Text="{x:Bind Timestamp, Converter={StaticResource TimestampToStringConverter}}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlDisabledBaseMediumBrush}" Margin="0,0,0,4"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>

                        <!-- Loading Indicator -->
                        <ProgressBar Grid.Row="1" IsIndeterminate="True" Visibility="{x:Bind ViewModel.IsTranscriptLoading, Converter={StaticResource BoolToVisibilityConverter}}" Margin="12"/>
                        <TextBlock Grid.Row="1" Text="Loading transcript..." Visibility="{x:Bind ViewModel.IsTranscriptLoading, Converter={StaticResource BoolToVisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,12"/>

                        <!-- Error Message -->
                        <TextBlock Grid.Row="1" Text="{x:Bind ViewModel.ErrorMessage}" Visibility="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" TextWrapping="Wrap" Margin="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <!-- Actions -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="12" Spacing="8">
                            <Button Content="Copy Transcript" Click="CopyTranscript_Click"/>
                            <Button Content="Export Transcript" Click="ExportTranscript_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
