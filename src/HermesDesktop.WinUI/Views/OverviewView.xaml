<Page
    x:Class="HermesDesktop.WinUI.Views.OverviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HermesDesktop.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:HermesDesktop.WinUI.ViewModels"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:OverviewViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="20">
            <TextBlock Text="Hermes Desktop Overview" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Margin="20">
            <StackPanel>
                <!-- Connection Info -->
                <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Connection" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding HostInfo}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding HermesProfileInfo}" TextWrapping="Wrap" Margin="0,0,0,4"/>
                        <TextBlock Text="{Binding ConnectionStatus}" TextWrapping="Wrap" Margin="0,0,0,4"
                                   Foreground="{Binding IsConnected, Converter={StaticResource BoolToBrushConverter}}"/>
                    </StackPanel>
                </Border>

                <!-- Discovered Profiles -->
                <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Discovered Hermes Profiles" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <ItemsControl ItemsSource="{Binding DiscoveredProfiles}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}" Margin="0,4,0,0"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <!-- Refresh Button -->
                <Button Content="Refresh Connection" 
                        Click="RefreshButton_Click"
                        HorizontalAlignment="Left"
                        Margin="0,16,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
