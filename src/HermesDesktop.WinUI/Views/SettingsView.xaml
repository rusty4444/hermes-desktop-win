<Page
    x:Class="HermesDesktop.WinUI.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HermesDesktop.WinUI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:HermesDesktop.WinUI.ViewModels"
    xmlns:models="using:HermesDesktop.WinUI.Models"
    mc:Ignorable="d">

    <Page.DataContext>
        <viewmodels:SettingsViewModel x:Name="ViewModel"/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="20">
            <TextBlock Text="Settings" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,0,0,10"/>
        </StackPanel>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" Margin="20">
            <StackPanel>
                <!-- Connection Profiles List -->
                <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Connection Profiles" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <ListView ItemsSource="{x:Bind ViewModel.ConnectionProfiles, Mode=OneWay}"
                                  SelectedItem="{x:Bind ViewModel.SelectedProfile, Mode=TwoWay}"
                                  Margin="0,0,0,8"
                                  Height="200">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:ConnectionProfile">
                                    <StackPanel Margin="0,4,0,4">
                                        <TextBlock Text="{x:Bind SshAlias}" Style="{ThemeResource BodyTextBlockStyle}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{x:Bind Host}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlDisabledBaseMediumBrush}" Margin="0,2,0,0"/>
                                        <TextBlock Text="{x:Bind User}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlDisabledBaseMediumBrush}" Margin="0,2,0,0"/>
                                        <TextBlock Text="{x:Bind Port}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlDisabledBaseMediumBrush}" Margin="0,2,0,0"/>
                                        <TextBlock Text="{x:Bind HermesProfile}" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlDisabledBaseMediumBrush}" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0" Spacing="8">
                            <Button Content="Set as Active" Click="SetAsActiveButton_Click"/>
                            <Button Content="Update" Click="UpdateButton_Click"/>
                            <Button Content="Remove" Click="RemoveButton_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Add/Edit Profile Form -->
                <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="{x:Bind ViewModel.SelectedProfile, Converter={StaticResource NullToStringConverter}, FallbackValue='Add New Connection', TargetNullValue='Add New Connection'}" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <Grid Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Alias:" Grid.Row="0" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="0" Grid.Column="1" Text="{x:Bind ViewModel.NewAlias, Mode=TwoWay}" PlaceholderText="SSH alias (e.g., hermes-host)" Margin="0,0,0,8"/>

                            <TextBlock Text="Host:" Grid.Row="1" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="1" Grid.Column="1" Text="{x:Bind ViewModel.NewHost, Mode=TwoWay}" PlaceholderText="Hostname or IP address" Margin="0,0,0,8"/>

                            <TextBlock Text="User:" Grid.Row="2" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="2" Grid.Column="1" Text="{x:Bind ViewModel.NewUser, Mode=TwoWay}" PlaceholderText="Username" Margin="0,0,0,8"/>

                            <TextBlock Text="Port:" Grid.Row="3" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="3" Grid.Column="1" Text="{x:Bind ViewModel.NewPort, Mode=TwoWay}" PlaceholderText="22" Margin="0,0,0,8"/>

                            <TextBlock Text="Hermes Profile:" Grid.Row="4" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBox Grid.Row="4" Grid.Column="1" Text="{x:Bind ViewModel.NewHermesProfile, Mode=TwoWay}" PlaceholderText="Leave empty for default" Margin="0,0,0,8"/>

                            <TextBlock Text="Password:" Grid.Row="5" Grid.Column="0" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <PasswordBox x:Name="NewPasswordBox" PlaceholderText="SSH password (optional)" Margin="0,0,0,8"/>
                        </Grid>
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0" Spacing="8">
                            <Button Content="Add" Click="AddButton_Click"/>
                            <Button Content="Clear" Click="ClearButton_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <!-- Status and Actions -->
                <Border BorderBrush="{ThemeResource SystemControlForegroundBaseMediumBrush}" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Status" Style="{ThemeResource SubtitleTextBlockStyle}" Margin="0,0,0,8"/>
                        <TextBlock Text="{x:Bind ViewModel.ErrorMessage}" TextWrapping="Wrap" Margin="0,0,0,4" Foreground="{ThemeResource SystemControlForegroundBaseHighBrush}" Visibility="{x:Bind ViewModel.ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
                        <TextBlock Text="Loading..." Margin="0,0,0,4" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        <ProgressBar IsIndeterminate="True" Margin="0,0,0,4" Visibility="{x:Bind ViewModel.IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
